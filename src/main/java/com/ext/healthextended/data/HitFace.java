package com.ext.healthextended.data;

/**
 * The face of a body part that was struck, expressed relative to the victim's
 * own orientation (i.e. FRONT = the face the victim is looking toward).
 */
public enum HitFace {
    FRONT,
    BACK,
    LEFT,
    RIGHT
}
